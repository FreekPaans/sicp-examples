#lang racket

(define (real-part z)
  '?)

(define (imag-part z)
  '?)

(define (magnitude z)
  '?)

(define (angle z)
  '?)

(define (make-from-real-imag x y)
  '?)

(define (make-from-mag-ang r a)
  '?)

